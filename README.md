# sitepoint-first-react-app
Contains files for the "Build Your First React App" course on SitePoint Premium

Success! Created first-react at /Applications/MAMP/htdocs/SitePoint/sitepoint-first-react-app/first-react
Inside that directory, you can run several commands:

### Commands for running the Application

* Starts the development server: `npm start`
* Bundles the app into static files for production: `npm run build`
* Starts the test runner: `npm test`
* Removes this tool and copies build dependencies, configuration files and scripts into the app directory: `npm run eject`. **If you do this, you can’t go back!**

### We suggest that you begin by typing:

`cd first-react`
 
`npm start`
